apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: willscharts-root
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/willpakpoy/willscharts.git
        revision: main
        directories:
          - path: charts/*
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/willpakpoy/willscharts.git
        targetRevision: main
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true