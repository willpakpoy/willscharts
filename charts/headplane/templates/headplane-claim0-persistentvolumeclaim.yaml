apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: headplane-claim0
  name: headplane-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
